// Shared app-wide constants.

/** The single show date (ISO yyyy-mm-dd). Used to bucket tasks and filter timeline slots. */
export const SHOW_DATE = '2026-08-16';

/** sortOrder below this value on the show date counts as "day of show" rather than "post-show". */
export const DAY_OF_SHOW_SORT_THRESHOLD = 300;
